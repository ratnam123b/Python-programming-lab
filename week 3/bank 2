ef bal(balance):
    print(balance)
    
def credit(balance,value):
        transactions.append(value)
        balance+=value
        return balance 
        
def credit(balance,value):
        transactions.append(-value)
        balance-=value
        return balance 
        
transactions=[]

def last_transactions:
    print("last 5 transactions:")
    for t in transaction[-5:]:
        if t<0:
            print(f "debited:{t}")
            else
            print(f" credited:{t}")
            
            balance=0
            print("1.credit")
            print("2.debit")
            print("3.show balance")
            print("4.show last 5"transactions)
                print(.exit)
            while true:
                try:
                a=int(input("enter the option:"))
                if a==1:
             x=int(input("ENTER THE AMOUNT TO BE CREDITED:")
              balance=credit(balance)
                elif a==2
            y=int(input("ENTER THE AMOUNT TO BE DEBITED:")
            balance=debit(balance,y)
            print("balance is",end="")bal(balance)
            elif a=3:
            print("balance is",end="")bal(balance)    
            elif a==4
            last_transaction()
            elif a==5
            print("enter....have a great day ")
            break
            else:
                print("Invalid option.please enter a number from 1 to 5.")
                except value error:
                    print ("please enter a valid number.")
        
                
        
        
